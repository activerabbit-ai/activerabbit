<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Reset Your Password - ActiveRabbit</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.5;
      color: #333;
      background-color: #f4f6f8;
      margin: 0;
      padding: 16px;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid #e5e5e5;
    }
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 16px;
      text-align: center;
    }
    .content {
      padding: 24px 20px;
    }
    p { margin: 0 0 16px 0; color: #333; font-size: 14px; }
    .note {
      background: #f3f4f6;
      border-radius: 6px;
      padding: 16px;
      margin: 16px 0;
      font-size: 13px;
      color: #6b7280;
    }
    .warning-box {
      background: #fef3c7;
      border: 1px solid #f59e0b;
      border-radius: 6px;
      padding: 16px;
      margin: 16px 0;
    }
    .button-wrapper {
      text-align: center;
      margin: 24px 0 8px;
    }
    .button {
      display: inline-block;
      padding: 12px 20px;
      background: #667eea;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 600;
    }
    .footer {
      text-align: center;
      padding: 16px;
      font-size: 13px;
      color: #777;
      border-top: 1px solid #e5e5e5;
    }
    .footer a { color: #667eea; text-decoration: none; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div style="text-align:center;">
        <div style="display:inline-flex;align-items:center;">
          <%= image_tag "faviicons/favicon-512.png",
            width: 48,
            height: 48,
            style: "display:block;margin-right:16px;" %>
          <div style="text-align:left;">
            <div style="font-weight:700;font-size:16px;line-height:1.25;">
              Reset Your Password
            </div>
            <div style="font-size:13px;opacity:0.9;margin-top:2px;">
              ActiveRabbit Account Security
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="content">
      <p>Hello <%= @resource.email %>,</p>

      <p>Someone requested a password reset for your ActiveRabbit account. If this was you, click the button below to set a new password:</p>

      <div class="button-wrapper">
        <%= link_to 'Reset My Password', edit_password_url(@resource, reset_password_token: @token), class: 'button', style: 'color: #ffffff !important;' %>
      </div>

      <div class="warning-box">
        <strong>⚠️ Security Notice:</strong><br>
        This link will expire in 6 hours. If you didn't request this reset, you can safely ignore this email.
      </div>

      <div class="note">
        If you didn't request a password reset, no action is needed. Your password will remain unchanged.
      </div>
    </div>

    <div class="footer">
      ActiveRabbit &ndash; AI-Powered Error Tracking for Rails
    </div>
  </div>
</body>
</html>
