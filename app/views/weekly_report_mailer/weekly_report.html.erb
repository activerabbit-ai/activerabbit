<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ActiveRabbit Weekly Report</title>

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.5;
      color: #333;
      background-color: #f4f6f8;
      margin: 0;
      padding: 16px;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid #e5e5e5;
    }

    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 16px;
      text-align: center;
    }

    .content {
      padding: 24px 20px;
    }

    h2 {
      margin: 24px 0 12px;
      font-size: 16px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 0 auto 12px;
    }

    th {
      text-align: left;
      font-size: 13px;
      color: #666;
      border-bottom: 1px solid #e5e5e5;
      padding: 6px 4px;
    }

    td {
      padding: 8px 4px;
      font-size: 14px;
      border-bottom: 1px solid #f0f0f0;
      vertical-align: top;
    }

    .right {
      text-align: right;
      white-space: nowrap;
    }

    .muted {
      color: #777;
      font-size: 12px;
    }

    .empty {
      background: #f8f9fa;
      padding: 12px;
      border-radius: 6px;
      font-size: 14px;
      color: #555;
      text-align: center;
    }

    .button-wrapper {
      text-align: center;
      margin: 24px 0 8px;
    }

    .button {
      display: inline-block;
      padding: 12px 20px;
      background: #667eea;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 600;
    }

    .footer {
      text-align: center;
      padding: 16px;
      font-size: 13px;
      color: #777;
      border-top: 1px solid #e5e5e5;
    }

    .chart-section {
      margin: 24px 0;
    }

    .chart-header {
      margin-bottom: 12px;
    }

    .chart-title {
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 4px;
    }

    .chart-total {
      font-size: 24px;
      font-weight: 700;
      color: #333;
      margin-bottom: 8px;
    }

    .chart-link {
      font-size: 13px;
      color: #667eea;
      text-decoration: none;
    }

    .chart-container {
      margin-top: 16px;
      padding: 12px 0;
    }

    .chart-bar-table {
      width: 100%;
      border-collapse: collapse;
    }

    .chart-bar-row {
      height: 32px;
    }

    .chart-bar-label-cell {
      width: 50px;
      font-size: 12px;
      color: #666;
      vertical-align: middle;
      padding-right: 8px;
    }

    .chart-bar-cell {
      vertical-align: middle;
      padding: 0;
    }

    .chart-bar-bg {
      height: 24px;
      background-color: #f0f0f0;
      border-radius: 3px;
      position: relative;
    }

    .chart-bar-fill {
      height: 24px;
      background-color: #667eea;
      border-radius: 3px;
      min-width: 2px;
    }

    .chart-bar-value {
      font-size: 11px;
      font-weight: 600;
      color: #333;
      padding-left: 8px;
      line-height: 24px;
    }

    .vchart {
      margin-top: 8px;
    }

    .vchart-table {
      width: 100%;
      border-collapse: collapse;
      table-layout: fixed;
    }

    .vchart-bars td {
      vertical-align: bottom;
      padding: 0 3px;
    }

    .vchart-bar-bg {
      height: 76px;
      background: #f0f0f0;
      border-radius: 4px;
      overflow: hidden;
    }

    .vchart-bar-fill {
      background: #667eea;
      border-radius: 4px;
      width: 100%;
      height: 100%;
      min-height: 2px;
    }

    .vchart-value {
      font-size: 10px;
      font-weight: 600;
      color: #333;
      text-align: center;
      margin-top: 6px;
      white-space: nowrap;
    }

    .vchart-label {
      font-size: 11px;
      color: #666;
      text-align: center;
      padding-top: 6px;
      white-space: nowrap;
    }

    .chart-grid {
      width: 100%;
      border-collapse: collapse;
      table-layout: fixed;
      margin: 0 auto 8px;
    }

    .chart-grid > tbody > tr > td {
      vertical-align: top;
      width: 50%;
    }

    .chart-card {
      border: 1px solid #eaeaea;
      border-radius: 8px;
      padding: 14px 14px 10px;
      background: #ffffff;
    }

    .chart-card + .chart-card {
      margin-left: 12px;
    }

    .chart-total {
      margin-bottom: 6px;
    }

    @media only screen and (max-width: 640px) {
      .chart-grid,
      .chart-grid > tbody,
      .chart-grid > tbody > tr,
      .chart-grid > tbody > tr > td {
        display: block !important;
        width: 100% !important;
      }

      .chart-grid > tbody > tr > td + td {
        margin-top: 12px !important;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="header">
      <div style="text-align:center;">
        <div style="display:inline-flex;align-items:center;">
          <%= image_tag "faviicons/favicon-512.png",
            width: 48,
            height: 48,
            style: "display:block;margin-right:16px;" %>

          <div style="text-align:left;">
            <div style="font-weight:700;font-size:16px;line-height:1.25;">
              ActiveRabbit Weekly Report
            </div>
            <div style="font-size:13px;opacity:0.9;margin-top:2px;">
              <%= @report[:period].first.strftime("%b %d, %Y") %> –
              <%= @report[:period].last.strftime("%b %d, %Y") %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="content">
      <% show_errors_chart = @report[:errors_by_day].present? && @report[:errors_by_day].any? %>
      <% show_performance_chart = @report[:performance_by_day].present? && @report[:performance_by_day].any? %>

      <% if show_errors_chart || show_performance_chart %>
        <table class="chart-grid" cellpadding="0" cellspacing="0" border="0">
          <tr>
            <td style="padding-right: 6px;">
              <% if show_errors_chart %>
                <div class="chart-card">
                  <div class="chart-header">
                    <div class="chart-title">Total Project Errors</div>
                    <div class="chart-total"><%= (@report[:total_errors] || 0).to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse %></div>
                    <a href="<%= @errors_url %>" class="chart-link">View All Errors</a>
                  </div>
                  <div class="chart-container">
                    <% max_errors = @report[:errors_by_day].values.max || 1 %>
                    <% max_bar_height = 76 %>
                    <div class="vchart">
                      <table class="vchart-table" cellpadding="0" cellspacing="0" border="0">
                        <tr class="vchart-bars">
                          <% @report[:errors_by_day].each do |date, count| %>
                            <% date_obj = date.is_a?(Date) ? date : Date.parse(date.to_s) %>
                            <% pct = max_errors.to_f > 0 ? ((count.to_f / max_errors.to_f) * 1.0) : 0 %>
                            <% bar_height = [(pct * max_bar_height).round, 2].max %>
                            <% spacer_height = [max_bar_height - bar_height, 0].max %>
                            <td>
                              <table cellpadding="0" cellspacing="0" border="0" style="width: 100%; height: <%= max_bar_height %>px;">
                                <tr>
                                  <td style="padding: 0; height: <%= spacer_height %>px;"></td>
                                </tr>
                                <tr>
                                  <td style="padding: 0; height: <%= bar_height %>px;">
                                    <div class="vchart-bar-fill"></div>
                                  </td>
                                </tr>
                              </table>
                              <div class="vchart-value"><%= count.to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse %></div>
                              <div class="vchart-label"><%= date_obj.strftime("%a") %></div>
                            </td>
                          <% end %>
                        </tr>
                      </table>
                    </div>
                  </div>
                </div>
              <% end %>
            </td>
            <td style="padding-left: 6px;">
              <% if show_performance_chart %>
                <div class="chart-card">
                  <div class="chart-header">
                    <div class="chart-title">Total Project Performance</div>
                    <div class="chart-total"><%= (@report[:total_performance] || 0).to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse %></div>
                    <a href="<%= @performance_url %>" class="chart-link">View All Performance</a>
                  </div>
                  <div class="chart-container">
                    <% max_performance = @report[:performance_by_day].values.max || 1 %>
                    <% max_bar_height = 76 %>
                    <div class="vchart">
                      <table class="vchart-table" cellpadding="0" cellspacing="0" border="0">
                        <tr class="vchart-bars">
                          <% @report[:performance_by_day].each do |date, count| %>
                            <% date_obj = date.is_a?(Date) ? date : Date.parse(date.to_s) %>
                            <% pct = max_performance.to_f > 0 ? ((count.to_f / max_performance.to_f) * 1.0) : 0 %>
                            <% bar_height = [(pct * max_bar_height).round, 2].max %>
                            <% spacer_height = [max_bar_height - bar_height, 0].max %>
                            <td>
                              <table cellpadding="0" cellspacing="0" border="0" style="width: 100%; height: <%= max_bar_height %>px;">
                                <tr>
                                  <td style="padding: 0; height: <%= spacer_height %>px;"></td>
                                </tr>
                                <tr>
                                  <td style="padding: 0; height: <%= bar_height %>px;">
                                    <div class="vchart-bar-fill"></div>
                                  </td>
                                </tr>
                              </table>
                              <div class="vchart-value"><%= count.to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse %></div>
                              <div class="vchart-label"><%= date_obj.strftime("%a") %></div>
                            </td>
                          <% end %>
                        </tr>
                      </table>
                    </div>
                  </div>
                </div>
              <% end %>
            </td>
          </tr>
        </table>
      <% end %>

      <h2>Top Errors</h2>
      <% if @report[:errors].any? %>
        <table>
          <tr>
            <th>Error</th>
            <th class="right">Count</th>
            <th>Last seen</th>
          </tr>
          <% @report[:errors].each do |issue| %>
            <tr>
              <td>
                <% error_url = issue.project ? project_slug_error_url(issue.project.slug, issue.id, host: ENV.fetch("APP_HOST", "localhost:3000")) : error_url(issue.id, host: ENV.fetch("APP_HOST", "localhost:3000")) %>
                <a href="<%= error_url %>" style="color: #667eea; text-decoration: none;">
                  <strong><%= issue.exception_class %></strong><br>
                  <span class="muted"><%= issue.controller_action %></span>
                </a>
              </td>
              <td class="right"><%= issue.occurrences %></td>
              <td><%= issue.last_seen.strftime("%Y-%m-%d %H:%M") %></td>
            </tr>
          <% end %>
        </table>
      <% else %>
        <div class="empty">🎉 No errors recorded this week</div>
      <% end %>

      <h2>Slowest Endpoints</h2>
      <% if @report[:performance].any? %>
        <table>
          <tr>
            <th>Target</th>
            <th class="right">Requests</th>
            <th class="right">Avg</th>
            <th class="right">Max</th>
          </tr>
          <% @report[:performance].each do |p| %>
            <tr>
              <td>
                <% if p.project_id && @performance_projects&.dig(p.project_id) %>
                  <% project = @performance_projects[p.project_id] %>
                  <% perf_url = project_slug_performance_action_detail_url(project.slug, target: p.target, host: ENV.fetch("APP_HOST", "localhost:3000")) %>
                <% else %>
                  <% perf_url = performance_action_detail_url(target: p.target, host: ENV.fetch("APP_HOST", "localhost:3000")) %>
                <% end %>
                <a href="<%= perf_url %>" style="color: #667eea; text-decoration: none;">
                  <%= p.target %>
                </a>
              </td>
              <td class="right"><%= p.requests %></td>
              <td class="right"><%= p.avg_ms.round %> ms</td>
              <td class="right"><%= p.max_ms.round %> ms</td>
            </tr>
          <% end %>
        </table>
      <% else %>
        <div class="empty">👍 No performance issues detected</div>
      <% end %>

      <div class="button-wrapper">
        <a href="<%= @dashboard_url %>" class="button">
          Go to Dashboard
        </a>
      </div>

    </div>

    <div class="footer">
      This report was generated automatically by ActiveRabbit
    </div>

  </div>
</body>
</html>
