<%# Placeholder partial — Task 22 will replace with the real onboarding status row.
    Renders a no-op div so Turbo Stream broadcasts in earlier tasks (e.g. Sentry::ImportService)
    can render without raising ActionView::MissingTemplate. -%>
<div data-onboarding-status-row data-kind="<%= local_assigns[:kind] %>"></div>
